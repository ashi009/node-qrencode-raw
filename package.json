{
  "name": "qrencode-raw",
  "version": "0.1.2",
  "author": "Xiaoyi Shi <ashi009@gmail.com>",
  "description": "A node.js wrapper for libqrencode, but instead of encoding data to image, it returns raw QR data in buffer, which allows you to do amazing things.",
  "repository": {
    "type": "git",
    "url": "https://github.com/ashi009/node-qrencode-raw.git"
  },
  "scripts": {
    "test": "mocha"
  },
  "engines": {
    "node": ">=0.8"
  },
  "devDependencies": {
    "mocha": "*",
    "should": "*"
  },
  "dependencies": {
    "bindings": "*",
    "nan": "*"
  }
}
